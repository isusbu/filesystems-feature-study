---
# file: playbooks/dbservers.yml
- name: Configure Database Servers
  hosts: dbservers
  become: yes
  vars:
    atype: "database"
  roles:
    - common
    - lttng
    - role: mysql
      tags: ['mysql']
    - role: sqlite
      tags: ['sqlite']
